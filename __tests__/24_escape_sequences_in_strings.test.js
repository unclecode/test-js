describe(`Lesson 24 escape sequences in strings`, () =>{
	test(`<code>myStr</code> should not contain any spaces`, () =>{
		//test goes here
	})
	test(`<code>myStr</code> should contain the strings <code>FirstLine</code>, <code>SecondLine</code> and <code>ThirdLine</code> (remember case sensitivity)`, () =>{
		//test goes here
	})
	test(`<code>FirstLine</code> should be followed by the newline character <code>\n</code>`, () =>{
		//test goes here
	})
	test(`<code>myStr</code> should contain a tab character <code>\t</code> which follows a newline character`, () =>{
		//test goes here
	})
	test(`<code>SecondLine</code> should be preceded by the backslash character <code>\</code>`, () =>{
		//test goes here
	})
	test(`There should be a newline character between <code>SecondLine</code> and <code>ThirdLine</code>`, () =>{
		//test goes here
	})
	test(`<code>myStr</code> should only contain characters shown in the instructions`, () =>{
		//test goes here
	})
})
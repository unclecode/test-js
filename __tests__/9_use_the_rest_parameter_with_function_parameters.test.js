describe(`Lesson 9 use the rest parameter with function parameters`, () =>{
	test(`The result of <code>sum(0,1,2)</code> should be 3`, () =>{
		//test goes here
	})
	test(`The result of <code>sum(1,2,3,4)</code> should be 10`, () =>{
		//test goes here
	})
	test(`The result of <code>sum(5)</code> should be 5`, () =>{
		//test goes here
	})
	test(`The result of <code>sum()</code> should be 0`, () =>{
		//test goes here
	})
	test(`The <code>sum</code> function should use the <code>...</code> rest parameter on the <code>args</code> parameter.`, () =>{
		//test goes here
	})
})
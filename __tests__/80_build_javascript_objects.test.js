describe(`Lesson 80 build javascript objects`, () =>{
	test(`<code>myDog</code> should contain the property <code>name</code> and it should be a <code>string</code>.`, () =>{
		//test goes here
	})
	test(`<code>myDog</code> should contain the property <code>legs</code> and it should be a <code>number</code>.`, () =>{
		//test goes here
	})
	test(`<code>myDog</code> should contain the property <code>tails</code> and it should be a <code>number</code>.`, () =>{
		//test goes here
	})
	test(`<code>myDog</code> should contain the property <code>friends</code> and it should be an <code>array</code>.`, () =>{
		//test goes here
	})
	test(`<code>myDog</code> should only contain all the given properties.`, () =>{
		//test goes here
	})
})
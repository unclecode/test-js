describe(`Lesson 7 catch use of assignment operator instead of equality operator`, () =>{
	test(`Your code should fix the condition so it checks for equality, instead of using assignment.`, () =>{
		//test goes here
	})
	test(`The condition should use either <code>==</code> or <code>===</code> to test for equality.`, () =>{
		//test goes here
	})
})
/**
The result of <code>sum(0,1,2)</code> should be 3
The result of <code>sum(1,2,3,4)</code> should be 10
The result of <code>sum(5)</code> should be 5
The result of <code>sum()</code> should be 0
The <code>sum</code> function should use the <code>...</code> rest parameter on the <code>args</code> parameter.
*/